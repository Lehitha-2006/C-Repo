#include<stdio.h>
int main()
{
    int X,Y;
    scanf("%d",&X);
    scanf("%d",&Y);
    if(X/Y>=30)
    {
        printf("YES");
    }
    else
    {
        printf("NO");
    }
    return 0;
}